<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    <uses-permission android:name="android.permission.SEND_SMS" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <!-- Needed to list 3rd-party apps to protect/block; Android 11+ hides other apps' details
         by default (package visibility). Fine for a sideloaded parental-control app that is
         never distributed via Play Store. -->
    <uses-permission
        android:name="android.permission.QUERY_ALL_PACKAGES"
        tools:ignore="QueryAllPackagesPermission" />

    <!-- largeHeap: this process runs a VPN packet relay, an accessibility service that periodically
         captures/downscales/JPEG-compresses full-resolution screenshots, an on-device MediaPipe
         image embedder, and Room, all at once, in the background, indefinitely. Without this, the
         default per-app heap growth limit on most devices is 256MB (see OtterlingApplication's
         crash handler picking up "OutOfMemoryError ... target footprint 268435456, growth limit
         268435456"); a normal peak (e.g. a screenshot capture landing while the VPN relay is busy)
         can exhaust that and take the whole app down. This raises the ceiling to whatever the
         device grants a large-heap app (device-dependent, generally 2-4x normal); it doesn't fix
         an actual unbounded leak if one exists, but it removes this specific crash for legitimate
         peak usage, which is what OtterlingApplication's crash log (message only, no stack trace
         surfaced off-device, see its own doc comment) gave no evidence of being. -->
    <application
        android:name=".OtterlingApplication"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:largeHeap="true"
        android:theme="@style/Theme.Otterling">
        <activity
            android:name=".MainActivity"
            android:launchMode="singleTop"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <receiver
            android:name=".knox.KnoxLicenseReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="com.samsung.android.knox.intent.action.KNOX_LICENSE_STATUS" />
            </intent-filter>
        </receiver>

        <receiver
            android:name=".admin.DeviceAdminReceiverImpl"
            android:exported="true"
            android:permission="android.permission.BIND_DEVICE_ADMIN">
            <meta-data
                android:name="android.app.device_admin"
                android:resource="@xml/device_admin" />
            <intent-filter>
                <action android:name="android.app.action.DEVICE_ADMIN_ENABLED" />
            </intent-filter>
        </receiver>

        <receiver
            android:name=".monitoring.BootCompletedReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>

        <receiver
            android:name=".monitoring.DebugUnsuspendReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="${applicationId}.DEBUG_UNSUSPEND" />
                <action android:name="${applicationId}.DEBUG_STRIP_ADMIN" />
                <action android:name="${applicationId}.DEBUG_DISABLE" />
                <action android:name="${applicationId}.DEBUG_PERMIT_A11Y" />
                <action android:name="${applicationId}.DEBUG_ALLOW_UNINSTALL" />
                <action android:name="${applicationId}.DEBUG_ENABLE_FILTER" />
                <action android:name="${applicationId}.DEBUG_REFRESH_BLOCKLIST" />
                <action android:name="${applicationId}.DEBUG_SEED_CUSTOM_BLOCK" />
                <action android:name="${applicationId}.DEBUG_SEED_BLOCK_APP" />
                <action android:name="${applicationId}.DEBUG_PROBE_DNS" />
            </intent-filter>
        </receiver>

        <service
            android:name=".monitoring.ProtectionEnforcementService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Parental control tamper-protection enforcement for family device management" />
        </service>

        <service
            android:name=".focus.FocusGuardAccessibilityService"
            android:exported="true"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/focus_guard_accessibility_service" />
        </service>

        <activity
            android:name=".focus.FrictionActivity"
            android:exported="false"
            android:excludeFromRecents="true"
            android:taskAffinity=""
            android:theme="@style/Theme.Otterling" />

        <activity
            android:name=".tamper.AccessibilityGuardActivity"
            android:exported="false"
            android:excludeFromRecents="true"
            android:taskAffinity=""
            android:lockTaskMode="if_whitelisted"
            android:theme="@style/Theme.Otterling" />

        <activity
            android:name=".tamper.DeviceRemovedActivity"
            android:exported="false"
            android:excludeFromRecents="true"
            android:taskAffinity=""
            android:theme="@style/Theme.Otterling" />

        <activity
            android:name=".focus.HabitProofActivity"
            android:exported="false"
            android:excludeFromRecents="true"
            android:taskAffinity=""
            android:launchMode="singleTask"
            android:theme="@style/Theme.Otterling" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <receiver
            android:name=".updates.UpdateInstallResultReceiver"
            android:exported="false" />

        <!-- Receives the filter-server's "poll now" push so Mac tamper alerts reach the partner in
             seconds instead of on MacTamperPollWorker's 15-minute floor. See MacTamperMessagingService. -->
        <service
            android:name=".alerts.MacTamperMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <service
            android:name=".content.VpnFilterService"
            android:exported="true"
            android:permission="android.permission.BIND_VPN_SERVICE"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Local on-device DNS content filtering VPN for family device management" />
            <intent-filter>
                <action android:name="android.net.VpnService" />
            </intent-filter>
        </service>
    </application>

</manifest>
