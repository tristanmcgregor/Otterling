# Copy to .env.local (gitignored) for local dev only -- see README.md.
# Must match the LOCKPROFILE_TOKEN a local lockprofile_service.py is running with.
VITE_DEV_TOKEN=
# Only needed if your local lockprofile_service.py isn't on the default 127.0.0.1:8091.
VITE_DEV_API_TARGET=
