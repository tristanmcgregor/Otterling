*.iml
.gradle/
.idea/
.kotlin/
.claude/
__pycache__/
.cursor/*
!.cursor/rules/
!.cursor/rules/**
build/
captures/
local.properties
app/build/
app/libs/*.jar
!app/libs/README.md

# Local release/debug APKs — publish via the update host, don't commit binaries.
*.apk

# macOS FocusLock (Swift Package Manager)
macos/FocusLock/.build/
macos/FocusLock/.swiftpm/
# publish_release.sh output -- a real signed build + manifest, not for git (see RELEASE.md).
macos/FocusLock/.release/

# Local scratch
phone-apps.txt

# Nested sibling checkouts / production mirrors — never tracked by this repo.
Otterling-Playstore/
Otterling - Production (DO NOT TOUCH)/

# filter-server runtime data -- generated per-deployment, never committed (the mitmproxy CA in
# particular is unique per server and is a private key, not just a public cert).
filter-server/.env
filter-server/work/
filter-server/conf/
filter-server/mitm-ca/
filter-server/__pycache__/
filter-server/ca/*.pem
!filter-server/ca/README.md
filter-server/updates/*
!filter-server/updates/README.md
!filter-server/updates/macos-manifest.example.json
filter-server/caddy-data/
filter-server/caddy-config/
filter-server/lockprofile-data/

filter-server/.ci-otterling-deploy-key

.DS_Store
filter-server/fleet/.env
